platform: linux

image_resource:
  type: docker-image
  source:
    repository: node

inputs:
- name: source-code
- name: created-components

run:
  path: sh
  args:
  - -exc
  - |
    cd source-code/concourse/test/api-test
    ls -ltrh created-components
    echo $DASHVID_API_ADDRESS
    npm config set loglevel warn
    npm install
    npm test