platform: linux

image_resource:
  type: docker-image
  source:
    repository: node

inputs:
- name: source-code
- name: created-components

run:
  path: sh
  args:
  - -exc
  - |
    sudo chmod 777 created-components/api
    created-components/api
    cd source-code/concourse/test/api-test
    echo $DASHVID_API_ADDRESS
    npm config set loglevel warn
    npm install
    npm test